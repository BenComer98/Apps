

// Resets a statboard to ignore user-entered values
function Reset() {
    const handleClick = () => {
        console.log("Add reset functions here");
    };

    return (
        <button onClick={handleClick}>Reset</button>
    );
}

export default Reset;